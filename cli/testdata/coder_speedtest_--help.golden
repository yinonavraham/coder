Run upload and download tests from your machine to a workspace

Usage:
  coder speedtest <workspace> [flags]

Flags:
  -d, --direct                    Specifies whether to wait for a direct connection before
                                  testing speed
      --direction one of up|down   Specifies whether to run in reverse mode where the client
                                  receives and the server sends (default down)
  -t, --time duration             Specifies the duration to monitor traffic (default 5s)
